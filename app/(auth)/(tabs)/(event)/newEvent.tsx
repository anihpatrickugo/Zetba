import React from 'react'
import * as UI from "@/components/common/index"

const newEvent = () => {
  return (
    <UI.Containner>
        <UI.CustomText size='lg'>newEvent</UI.CustomText>
    </UI.Containner>
  )
}

export default newEvent
